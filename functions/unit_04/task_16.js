// Напишите стрелочную функцию t16, которая проверяет что в массиве находится четное количество элементов.
// Возвращает true, false.

const t16 = arr => !(arr.length % 2);

console.log(t16([4, 5, 6]));
